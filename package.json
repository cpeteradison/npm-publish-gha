{
  "name": "npm-publish-gha",
  "private": true,
  "version": "0.1.0",
  "description": "Example repo for testing npm trusted publishing with GitHub Actions.",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
